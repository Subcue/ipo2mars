node_modules/
.wrangler/
dist/
*.log
.dev.vars
.env
.DS_Store

# Build artifacts — regenerated by `npm run build` (Workers Builds runs it in CI).
public/styles.css
public/assets/

# Local launch kit (strategy + media, never published)
launch/
